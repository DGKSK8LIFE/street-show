CREATE TABLE User (
	username text,
	name text,
	email text,
	id int NOT NULL AUTO_INCREMENT,
	PRIMARY KEY (id)
);

CREATE TABLE Busker (
	username text,
	name text,
	email text,
	id int NOT NULL AUTO_INCREMENT,
	PRIMARY KEY (id)
);
